// Smooth Scroll for Anchor Links
document.querySelectorAll('nav a').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
      e.preventDefault();
      const target = document.querySelector(this.getAttribute('href'));
      target.scrollIntoView({ behavior: 'smooth' });
    });
  });

  // Disable pinch zoom (gesture events for touch devices)
document.addEventListener('touchstart', function (e) {
  if (e.touches.length > 1) {
    e.preventDefault(); // Prevent pinch zoom
  }
}, { passive: false });

document.addEventListener('gesturestart', function (e) {
  e.preventDefault(); // Prevent pinch zoom
}, { passive: false });

document.addEventListener('gesturechange', function (e) {
  e.preventDefault(); // Prevent zoom during gesture
}, { passive: false });

document.addEventListener('gestureend', function (e) {
  e.preventDefault(); // Prevent zoom after gesture
}, { passive: false });
  
  document.addEventListener('DOMContentLoaded', () => {
    const toggleBtn = document.querySelector('.toggle-btn');
    const content = document.querySelector('.content');
  
    toggleBtn.addEventListener('click', () => {
      content.classList.toggle('open'); // Toggle content visibility
      toggleBtn.classList.toggle('active'); // Toggle icon rotation
    });
  });
 
  document.addEventListener('DOMContentLoaded', () => {
    const toggleBtn = document.querySelector('.toggle-btnn');
    const content = document.querySelector('.contentt');
  
    toggleBtn.addEventListener('click', () => {
      content.classList.toggle('open'); // Toggle content visibility
      toggleBtn.classList.toggle('active'); // Toggle icon rotation
    });

  });

  document.addEventListener('DOMContentLoaded', () => {
    const toggleBtn = document.querySelector('.toggle-btnnn');
    const content = document.querySelector('.contenttt');
  
    toggleBtn.addEventListener('click', () => {
      content.classList.toggle('open'); // Toggle content visibility
      toggleBtn.classList.toggle('active'); // Toggle icon rotation
    });
  });

  document.addEventListener('DOMContentLoaded', () => {
    const toggleBtn = document.querySelector('.toggle-btnnnn');
    const content = document.querySelector('.contentttt');
  
    toggleBtn.addEventListener('click', () => {
      content.classList.toggle('open'); // Toggle content visibility
      toggleBtn.classList.toggle('active'); // Toggle icon rotation
    });
  });

  document.addEventListener('DOMContentLoaded', () => {
    const toggleBtn = document.querySelector('.toggle-btnnnnn');
    const content = document.querySelector('.contenttttt');
  
    toggleBtn.addEventListener('click', () => {
      content.classList.toggle('open'); // Toggle content visibility
      toggleBtn.classList.toggle('active'); // Toggle icon rotation
    });
  });

  document.addEventListener('DOMContentLoaded', () => {
    const toggleBtn = document.querySelector('.toggle-btntomorecoffee');
    const content = document.querySelector('.productsnameswithrecpies');
  
    toggleBtn.addEventListener('click', () => {
      content.classList.toggle('open'); // Toggle content visibility
      toggleBtn.classList.toggle('active'); // Toggle icon rotation
    });
  });
  
  // for  tea expender is here 
  // for  tea expender is here 
  // for  tea expender is here 
  // for  tea expender is here 
  // for  tea expender is here 
  // for  tea expender is here 
  // for  tea expender is here 
  // for  tea expender is here 
  // for  tea expender is here 
  // for  tea expender is here 
  // for  tea expender is here 
  // for  tea expender is here 
  // for  tea expender is here 
  document.addEventListener('DOMContentLoaded', () => {
    const toggleBtn = document.querySelector('.toggle-btntea');
    const content = document.querySelector('.contenttea');
  
    toggleBtn.addEventListener('click', () => {
      content.classList.toggle('open'); // Toggle content visibility
      toggleBtn.classList.toggle('active'); // Toggle icon rotation
    });
  });
 
  document.addEventListener('DOMContentLoaded', () => {
    const toggleBtn = document.querySelector('.toggle-btnteaa');
    const content = document.querySelector('.contentteaa');
  
    toggleBtn.addEventListener('click', () => {
      content.classList.toggle('open'); // Toggle content visibility
      toggleBtn.classList.toggle('active'); // Toggle icon rotation
    });

  });

  document.addEventListener('DOMContentLoaded', () => {
    const toggleBtn = document.querySelector('.toggle-btnteaaa');
    const content = document.querySelector('.contentteaaa');
  
    toggleBtn.addEventListener('click', () => {
      content.classList.toggle('open'); // Toggle content visibility
      toggleBtn.classList.toggle('active'); // Toggle icon rotation
    });
  });

  document.addEventListener('DOMContentLoaded', () => {
    const toggleBtn = document.querySelector('.toggle-btnteaaaa');
    const content = document.querySelector('.contentteaaaa');
  
    toggleBtn.addEventListener('click', () => {
      content.classList.toggle('open'); // Toggle content visibility
      toggleBtn.classList.toggle('active'); // Toggle icon rotation
    });
  });

  document.addEventListener('DOMContentLoaded', () => {
    const toggleBtn = document.querySelector('.toggle-btnteaji');
    const content = document.querySelector('.contentteaji');
  
    toggleBtn.addEventListener('click', () => {
      content.classList.toggle('open'); // Toggle content visibility
      toggleBtn.classList.toggle('active'); // Toggle icon rotation
    });
  });


  document.addEventListener('DOMContentLoaded', () => {
    const toggleBtn = document.querySelector('.toggle-btnteajii');
    const content = document.querySelector('.contentteajii');
  
    toggleBtn.addEventListener('click', () => {
      content.classList.toggle('open'); // Toggle content visibility
      toggleBtn.classList.toggle('active'); // Toggle icon rotation
    });
  });

  document.addEventListener('DOMContentLoaded', () => {
    const toggleBtn = document.querySelector('.toggle-btnpb');
    const content = document.querySelector('.contentpb');
  
    toggleBtn.addEventListener('click', () => {
      content.classList.toggle('open'); // Toggle content visibility
      toggleBtn.classList.toggle('active'); // Toggle icon rotation
    });
  });
  document.addEventListener('DOMContentLoaded', () => {
    const toggleBtn = document.querySelector('.toggle-btnpbb');
    const content = document.querySelector('.contentpbb');
  
    toggleBtn.addEventListener('click', () => {
      content.classList.toggle('open'); // Toggle content visibility
      toggleBtn.classList.toggle('active'); // Toggle icon rotation
    });
  });
  document.addEventListener('DOMContentLoaded', () => {
    const toggleBtn = document.querySelector('.toggle-btnpbbb');
    const content = document.querySelector('.contentpbbb');
  
    toggleBtn.addEventListener('click', () => {
      content.classList.toggle('open'); // Toggle content visibility
      toggleBtn.classList.toggle('active'); // Toggle icon rotation
    });
  });
  document.addEventListener('DOMContentLoaded', () => {
    const toggleBtn = document.querySelector('.toggle-btnpbbbb');
    const content = document.querySelector('.contentpbbbb');
  
    toggleBtn.addEventListener('click', () => {
      content.classList.toggle('open'); // Toggle content visibility
      toggleBtn.classList.toggle('active'); // Toggle icon rotation
    });
  });
  document.addEventListener('DOMContentLoaded', () => {
    const toggleBtn = document.querySelector('.toggle-btnpbbbbb');
    const content = document.querySelector('.contentpbbbbb');
  
    toggleBtn.addEventListener('click', () => {
      content.classList.toggle('open'); // Toggle content visibility
      toggleBtn.classList.toggle('active'); // Toggle icon rotation
    });
  });
  document.addEventListener('DOMContentLoaded', () => {
    const toggleBtn = document.querySelector('.toggle-btnpbbbbbb');
    const content = document.querySelector('.contentpbbbbbb');
  
    toggleBtn.addEventListener('click', () => {
      content.classList.toggle('open'); // Toggle content visibility
      toggleBtn.classList.toggle('active'); // Toggle icon rotation
    });
  });
  document.addEventListener('DOMContentLoaded', () => {
    const toggleBtn = document.querySelector('.toggle-btnpbbbbbbb');
    const content = document.querySelector('.contentpbbbbbbb');
  
    toggleBtn.addEventListener('click', () => {
      content.classList.toggle('open'); // Toggle content visibility
      toggleBtn.classList.toggle('active'); // Toggle icon rotation
    });
  });


  document.addEventListener('DOMContentLoaded', () => {
    const toggleBtn = document.querySelector('.toggle-btnob');
    const content = document.querySelector('.contentob');
  
    toggleBtn.addEventListener('click', () => {
      content.classList.toggle('open'); // Toggle content visibility
      toggleBtn.classList.toggle('active'); // Toggle icon rotation
    });
  });
  document.addEventListener('DOMContentLoaded', () => {
    const toggleBtn = document.querySelector('.toggle-btnobb');
    const content = document.querySelector('.contentobb');
  
    toggleBtn.addEventListener('click', () => {
      content.classList.toggle('open'); // Toggle content visibility
      toggleBtn.classList.toggle('active'); // Toggle icon rotation
    });
  });
  document.addEventListener('DOMContentLoaded', () => {
    const toggleBtn = document.querySelector('.toggle-btnobbb');
    const content = document.querySelector('.contentobbb');
  
    toggleBtn.addEventListener('click', () => {
      content.classList.toggle('open'); // Toggle content visibility
      toggleBtn.classList.toggle('active'); // Toggle icon rotation
    });
  });
  document.addEventListener('DOMContentLoaded', () => {
    const toggleBtn = document.querySelector('.toggle-btnobbbb');
    const content = document.querySelector('.contentobbbb');
  
    toggleBtn.addEventListener('click', () => {
      content.classList.toggle('open'); // Toggle content visibility
      toggleBtn.classList.toggle('active'); // Toggle icon rotation
    });
  });
  document.addEventListener('DOMContentLoaded', () => {
    const toggleBtn = document.querySelector('.toggle-btnobbbbb');
    const content = document.querySelector('.contentobbbbb');
  
    toggleBtn.addEventListener('click', () => {
      content.classList.toggle('open'); // Toggle content visibility
      toggleBtn.classList.toggle('active'); // Toggle icon rotation
    });
  });

  document.addEventListener('DOMContentLoaded', () => {
    const toggleBtn = document.querySelector('.toggle-btntomorecoffeee');
    const content = document.querySelector('.productsnameswithrecpiess');
  
    toggleBtn.addEventListener('click', () => {
      content.classList.toggle('open'); // Toggle content visibility
      toggleBtn.classList.toggle('active'); // Toggle icon rotation
    });
  });

  document.addEventListener('DOMContentLoaded', () => {
    const toggleBtn = document.querySelector('.toggle-btntomorepb');
    const content = document.querySelector('.productsnameswithrecpiesss');
  
    toggleBtn.addEventListener('click', () => {
      content.classList.toggle('open'); // Toggle content visibility
      toggleBtn.classList.toggle('active'); // Toggle icon rotation
    });
  });

  document.addEventListener('DOMContentLoaded', () => {
    const toggleBtn = document.querySelector('.toggle-btntomoreob');
    const content = document.querySelector('.productsnameswithrecpiessss');
  
    toggleBtn.addEventListener('click', () => {
      content.classList.toggle('open'); // Toggle content visibility
      toggleBtn.classList.toggle('active'); // Toggle icon rotation
    });
  });

  document.addEventListener('DOMContentLoaded', () => {
    const toggleBtn = document.querySelector('.onlyforhovermain');
    const content = document.querySelector('.openmenuforshow');
  
    toggleBtn.addEventListener('click', () => {
      content.classList.toggle('open'); // Toggle content visibility
      toggleBtn.classList.toggle('active'); // Toggle icon rotation
      toggleBtn
    });
  });
  
  document.addEventListener('DOMContentLoaded', () => {
    const toggleBtn = document.querySelector('.onlyforhovermain'); // Select the element
    
    toggleBtn.addEventListener('click', () => {
      // Check the current text and toggle between two values
      if (toggleBtn.textContent === 'Close Menu') {
        toggleBtn.textContent = 'Show Menu';
      } else {
        toggleBtn.textContent = 'Close Menu';
      }
    });
  });
  const menuIcon = document.getElementById('menuIcon');
const sideMenu = document.getElementById('sideMenu');
const closeIcon = document.getElementById('closeIcon');

menuIcon.addEventListener('click', () => {
  // Open the sidebar and show the close icon
  sideMenu.classList.add('open');
});

closeIcon.addEventListener('click', () => {
  // Close the sidebar and hide the close icon
  sideMenu.classList.remove('open');
});
document.querySelector('.menubotton').addEventListener('click', function() {
  this.classList.add('clicked');
  setTimeout(() => {
    this.classList.remove('clicked');
  }, 300); // Match the animation duration
});
// imageslider
// imageslider
// imageslider
// imageslider
// imageslider
function scrollToAbout() {
  const aboutSection = document.getElementById('about');
  aboutSection.scrollIntoView({ behavior: 'smooth' });
}
let slideIndex = 0;
const slides = document.querySelectorAll('.slide');

function showSlide(index) {
  if (index >= slides.length) slideIndex = 0;
  if (index < 0) slideIndex = slides.length - 1;

  // Hide all slides
  slides.forEach((slide) => slide.classList.remove('fade'));

  // Show the current slide
  slides[slideIndex].classList.add('fade');
}

function moveSlide(step) {
  slideIndex += step;
  showSlide(slideIndex);
}

// Automatically change slides every 3 seconds
setInterval(() => {
  moveSlide(1);
}, 3000);

// Show the first slide
showSlide(slideIndex);